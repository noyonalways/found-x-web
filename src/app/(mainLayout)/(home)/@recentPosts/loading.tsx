import Container from "@/components/ui/container";

interface IProps {}

const Loading: React.FC<IProps> = () => {
  return (
    <Container>
      <h1> Loading... </h1>
    </Container>
  );
};

export default Loading;
